package main

func getDataForCharacter(steamID string, slot uint8) {
	// Read from SQLite.
}

func saveDataForCharacter(steamID string, slot uint8, buffer string) {
	// Parse JSON buffer, save the data to SQLite for the desired char.
}
